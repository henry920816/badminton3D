# 品質檢查：純 2D 修復版

以 GitHub main abdc210486b7433b991a868cdd6b0d4e2053c487 為基準，包含前次品質檢查改版、只顯示紅色，以及本次純 2D 修復與復原修正。

## 目前行為

- 「修正 2D」只修改品質檢查判錯、且通過修復回驗的相機 2D 標註。
- 其他正確視角的 2D 標註保持原值。
- 自動修復及其復原都不讀寫 BallTraj，不新增、修改或刪除 3D 軌跡；前端也不更新 3D 軌跡快取。
- 仍會以其他可靠 2D 視角和相機參數計算暫時的幾何位置，再投影到需要修復的相機；這只是推算 2D 位置的中間計算，不使用或保存到既有 3D 軌跡。
- 不再使用既有 3D 軌跡作為時間參考。缺少可靠的多視角支持就跳過，不從 3D 軌跡補點。
- 「復原上次 2D 修復」一次還原最後一批修正的 2D。若相關 2D 已有後續修改，整批取消，不只還原一半。
- 「只顯示紅色」隱藏非紅色狀態格子；不刪資料、不更動演算法或修復範圍。

## 品質檢查顏色

- 綠：多視角一致。
- 紅：明確資料錯誤或偏離重建投影。
- 橘：多視角衝突，无法唯一定位錯誤視角。
- 紫：視角不足、參數缺失或幾何不穩定，無法驗證。
- 灰：沒有標註。

懸停格子可看原因與可用的重投影誤差。預設重投影門檻 12 px、最小視線夾角 1 度，設定於 backend/app/triangulation.py。至少三個視角支持且最大支持集合唯一時形成共識。未依實際比賽標註校準最佳門檻。此版不做時間平滑；若各視角一致指向同一錯誤物體，幾何檢查仍可能通過。

## 更新執行

1. 關閉前端；將 ZIP 內 badminton3D 的內容覆蓋至原專案目錄，沿用原 Docker Compose 專案名稱與 volumes。
2. 啟動 Docker Desktop，在原專案根目錄執行：

```powershell
docker compose up --build -d
docker compose restart backend
```

3. 另開終端機：

```powershell
cd frontend
npm ci
npm run dev
```

4. 網頁 Ctrl+F5，重新選擇影片，開啟品質檢查重新掃描。

不需重新建立資料集。不要執行 docker compose down -v。

## 復原紀錄範圍

新版紀錄使用 auto_2d_only，與舊版 2D＋3D 修復隔離。舊的復原入口無法誤用新版純 2D 紀錄；新版復原按鈕也不會帶入舊版整批復原紀錄。

按鈕只記住本版最後一批成功修復，按資料集區分，記錄於同一分頁的 sessionStorage。重新整理／切換檢視仍可使用，關閉分頁後不保證保留；沒有重做或多步歷史介面。瀏覽器禁止儲存時，紀錄僅保留於當前面板。

本次更新不會自動還原舊版已經修改的 3D 資料。原專案獨立的 3D 軌跡插值、多視角手動 2D→3D 修復功能仍在；這兩個明確操作 3D 的功能不屬於品質檢查的純 2D 自動修復。

## 驗證與內容

- Python 共 26 項測試通過。
- 包含完整 3D 欄位快照驗證：預覽、修復、復原前後一致。
- 包含完全沒有 3D 軌跡時的 2D 修復／復原，以及其他正確相機 2D 標註不變。
- 包含純 2D 復原不刪 3D、舊版紀錄拒絕、新版紀錄拒絕被舊 3D 復原誤用、整批衝突 rollback。
- 前端正式版建置通過，仍有原本的 bundle 大小及混合 import 提醒。
- 尚未執行使用者實際比賽資料、PostgreSQL 與瀏覽器端到端驗證。
- ZIP 含完整前後端、設定、工具、模型資產與測試；不含 .git、node_modules、建置輸出、快取、私人影片或資料庫。
